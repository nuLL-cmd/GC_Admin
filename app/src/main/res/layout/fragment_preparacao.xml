<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:background="#FB8C00"
    android:layout_height="match_parent">

    <!-- TODO: Update blank fragment layout -->

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/recycler_delivered"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_below="@id/txt_delivered_delivered"
        android:layout_marginTop="5dp"
        android:layout_marginBottom="5dp"
        tools:listitem="@layout/layout_celula_pedidos" />

    <TextView
        android:id="@+id/txt_delivered_delivered"
        android:layout_width="wrap_content"
        android:layout_centerHorizontal="true"
        android:layout_height="wrap_content"
        android:textColor="#fff"
        android:layout_marginTop="10dp"
        android:text="Preparação"
        android:textSize="20sp"
        android:textStyle="bold" />
    <ImageView
        android:layout_width="32dp"
        android:layout_height="32dp"
        android:src="@drawable/ic_cardapio_sgv"
        android:layout_toStartOf="@id/txt_delivered_delivered"
        android:layout_alignTop="@id/txt_delivered_delivered"
        android:layout_alignBottom="@id/txt_delivered_delivered"
        android:layout_marginEnd="5dp"/>
</RelativeLayout>